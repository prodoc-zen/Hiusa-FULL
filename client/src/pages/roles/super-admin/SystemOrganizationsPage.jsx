import { useEffect, useState } from 'react';
import { Building2, PencilLine, Plus, Search, Users } from 'lucide-react';
import { createSystemOrganization, getSystemOrganizations, updateSystemOrganization } from '../../../services/systemAdministrationService';

const empty = { name: '', acronym: '', college: '', description: '', is_active: true };

export default function SystemOrganizationsPage() {
  const [items, setItems] = useState([]); const [search, setSearch] = useState(''); const [form, setForm] = useState(null); const [busy, setBusy] = useState(false); const [error, setError] = useState('');
  const load = () => getSystemOrganizations({ search, per_page: 100 }).then((data) => setItems(data.data || [])).catch(() => setError('Unable to load organizations.'));
  useEffect(() => { load(); }, []);
  const save = async (event) => { event.preventDefault(); setBusy(true); setError(''); try { form.id ? await updateSystemOrganization(form.id, form) : await createSystemOrganization(form); setForm(null); load(); } catch (e) { setError(e.response?.data?.message || 'Could not save the organization.'); } finally { setBusy(false); } };
  return <div className="space-y-6"><section className="flex flex-col gap-4 rounded-2xl bg-[#0B1831] p-6 text-white shadow-sm sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-[#16C7F3]">SAO administration</p><h2 className="mt-2 text-2xl font-black">Organizations</h2><p className="mt-1 text-sm text-slate-300">Register, assign, and monitor student organizations without entering their operations.</p></div><button onClick={() => setForm(empty)} className="inline-flex h-11 items-center justify-center gap-2 rounded-lg bg-[#16C7F3] px-4 text-sm font-bold text-[#062033]"><Plus size={17} /> Add organization</button></section><div className="rounded-xl border border-[#DDE7EF] bg-white p-4"><div className="flex gap-2"><label className="flex h-10 flex-1 items-center gap-2 rounded-lg border border-[#DDE7EF] px-3"><Search size={16} className="text-slate-400" /><input value={search} onChange={(e) => setSearch(e.target.value)} onKeyDown={(e) => e.key === 'Enter' && load()} placeholder="Search name, code, or college" className="w-full outline-none" /></label><button onClick={load} className="rounded-lg bg-slate-900 px-4 text-sm font-bold text-white">Search</button></div></div>{error && <p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<section className="grid gap-4 lg:grid-cols-2">{items.map((org) => <article key={org.id} className="rounded-xl border border-[#DDE7EF] bg-white p-5 shadow-sm"><div className="flex justify-between gap-3"><div className="flex gap-3"><span className="grid h-10 w-10 place-items-center rounded-lg bg-[#E6F6FD] text-[#0B8ED0]"><Building2 size={19} /></span><div><h3 className="font-bold text-slate-900">{org.name}</h3><p className="text-xs font-semibold text-slate-500">{org.acronym} · {org.college || 'No department assigned'}</p></div></div><span className={`h-fit rounded-full px-2 py-1 text-[10px] font-bold ${org.is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600'}`}>{org.is_active ? 'ACTIVE' : 'INACTIVE'}</span></div><p className="mt-4 min-h-10 text-sm text-slate-600">{org.description || 'No organization description yet.'}</p><div className="mt-4 flex items-center justify-between border-t border-slate-100 pt-3 text-xs font-semibold text-slate-500"><span className="inline-flex items-center gap-1"><Users size={14} /> {org.users_count} members · {org.administrators?.length || 0} admins</span><button onClick={() => setForm(org)} className="inline-flex items-center gap-1 text-[#0B8ED0]"><PencilLine size={14} /> Edit</button></div></article>)}</section>{!items.length && <div className="rounded-xl border border-dashed border-slate-300 p-10 text-center text-sm text-slate-500">No student organizations match this view.</div>}{form && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/50 p-4"><form onSubmit={save} className="max-h-[90vh] w-full max-w-lg overflow-y-auto rounded-2xl bg-white p-6 shadow-2xl"><h3 className="text-lg font-black text-slate-900">{form.id ? 'Edit organization' : 'Add organization'}</h3><div className="mt-5 grid gap-3">{[['name', 'Organization name'], ['acronym', 'Organization code'], ['college', 'Department / college']].map(([key, label]) => <label key={key} className="text-sm font-semibold text-slate-700">{label}<input required={key !== 'college'} value={form[key] || ''} onChange={(e) => setForm({ ...form, [key]: e.target.value })} className="mt-1 h-10 w-full rounded-lg border border-slate-200 px-3 font-normal outline-[#0B8ED0]" /></label>)}<label className="text-sm font-semibold text-slate-700">Description<textarea value={form.description || ''} onChange={(e) => setForm({ ...form, description: e.target.value })} className="mt-1 min-h-24 w-full rounded-lg border border-slate-200 p-3 font-normal outline-[#0B8ED0]" /></label><label className="flex items-center gap-2 text-sm font-semibold text-slate-700"><input type="checkbox" checked={Boolean(form.is_active)} onChange={(e) => setForm({ ...form, is_active: e.target.checked })} /> Active organization</label></div><div className="mt-6 flex justify-end gap-2"><button type="button" onClick={() => setForm(null)} className="rounded-lg px-4 py-2 text-sm font-bold text-slate-600">Cancel</button><button disabled={busy} className="rounded-lg bg-[#0B8ED0] px-4 py-2 text-sm font-bold text-white">{busy ? 'Saving…' : 'Save organization'}</button></div></form></div>}</div>;
}
